# P01 — Select a canonical conductor track

Read `conductor/manifest.json`, `.conductor/local/conductor_status.md`, and `.conductor/local/track_state.json`. Select one dependency-ready `C##` track. Record the status as `in_progress`, the intended files, and the evidence that will demonstrate completion. Do not create another numbered track.
