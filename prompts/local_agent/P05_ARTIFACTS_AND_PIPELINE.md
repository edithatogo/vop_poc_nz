# P05 — Reproducible artifacts and pipeline

Wire result manifests, hashes, seeds, environment metadata, method-contract version, and artifact-promotion state through generated tables and figures. Keep raw/private inputs and exploratory outputs local. Do not use pickle as the public interchange format.
