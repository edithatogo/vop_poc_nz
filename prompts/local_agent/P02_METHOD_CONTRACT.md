# P02 — Lock method semantics before implementation

For EVoP/PAF work, read the versioned method contracts and shared conformance fixture. Confirm current-information versus per-draw semantics, source/evaluation direction, tie policy, population scaling, exact frontier behaviour, and Monte Carlo assurance. Stop if the live implementation uses perspectives as decision alternatives or silently breaks ties by array order.
